# Application icons

`source-icon.svg` is the canonical Ingot Agent application mark. Native PNG, ICNS, and ICO files are generated from it and are not edited independently.

```bash
npm run icons
```

Run the command whenever the canonical SVG changes, then review every generated platform size before release packaging.

