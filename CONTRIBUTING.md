# 参与 Ingot 开发

[English](CONTRIBUTING.en.md)

感谢你为 Ingot 提交代码、测试、文档或设计建议。Ingot 的目标是通过融合实验数据、实时过程数据、物理机理和专家知识，辅助工艺工程师更快完成实验设计、规律发现、参数优化和工艺窗口验证。

所有变更都应服务于这一目标，并保持中英文文档一致。

## 工程原则

- 以工艺研发项目为核心组织目标、实验、过程数据、分析结论、验证结果和知识沉淀。
- 原始数据、计算过程、模型版本和结论依据可追溯、可复现。
- 数据采集是系统的基础能力。通用协议由 Edge 驱动实现，设备差异通过可版本化的设备契约和适配层处理。
- `Ingot.Domain`、`Ingot.Edge.Application` 和 `Ingot.Agent` 不依赖具体数据库、模型供应商或设备协议。
- 物理机理、统计分析、优化算法和专家规则通过稳定契约协作，避免把某一种方法写死在核心流程中。
- AI 负责理解任务、组织分析和解释结果；确定性代码负责数据校验、权限、计算执行、运行边界和证据关联。
- 工艺工程师保留实验审批、参数发布和工艺窗口确认权。
- 公共契约使用明确的类型、单位、时间语义、质量状态和版本，不依赖隐式兼容。

## 本地环境

要求：

- .NET SDK 10
- Node.js 22.13 或更高版本
- Docker 与 Docker Compose

安装依赖：

```bash
dotnet restore Ingot.sln
npm --prefix src/platform/Ingot.Platform.Web ci
npm --prefix apps/website ci
npm --prefix apps/docs-site ci
```

各应用的启动方式见仓库根目录的 `AGENTS.md` 和相应应用目录中的 README。

## 变更要求

### 工艺研发领域

- 新能力必须说明它服务于工艺研发闭环的哪个环节，以及输入、输出和验证方式。
- 实验、样品、设备、材料、配方、工艺参数、质量结果和分析运行之间的关系应使用稳定标识表达。
- 关键数字和结论必须关联到真实数据、计算方法、运行版本和适用条件。
- 工艺窗口必须同时表达目标、约束、可行域、置信度和验证状态。

### 数据采集与设备适配

- 通用协议驱动负责连接、读取、写入、订阅、重连和错误分类等协议能力。
- 设备契约负责地址映射、数据类型、单位、缩放、时间戳、质量状态和语义名称。
- 采集运行时负责缓冲、去重、顺序、断点续传、可观测性和安全边界。
- 新协议或设备适配必须包含契约样例、模拟测试、异常恢复测试和现场验收条件。
- 协议或厂商模型只存在于适配边界，不进入工艺研发核心领域模型。

### AI、算法与机理

- 核心只依赖 `IModelClient`、`IAnalysisTool` 和其他稳定接口。
- 模型输出必须是强类型结果，并在执行前经过确定性校验。
- 分析工具必须声明版本、输入输出结构、单位要求、超时、取消、资源上限和结果溯源信息。
- 新算法应提供基线对照、适用前提、失败条件和可复现实验。
- AI 生成的建议应清楚区分数据事实、模型推断和待验证假设。

### API 与存储

- API 输入在边界完成类型、租户和权限校验。
- 数据库改动包含迁移、索引、并发规则、失败处理和集成测试。
- 实验数据、实时过程数据、分析产物和知识条目保留来源、时间、版本与质量信息。
- 日志、指标和追踪不得记录密钥、完整提示或敏感业务数据。

### Web 与文档

- 页面以工艺工程师的工作流程组织，不以底层服务或数据库结构组织。
- 关键任务应有清晰的入口、当前状态、下一步操作、完成反馈和错误恢复路径。
- 官网、README 和公开文档用于介绍项目价值、核心能力、工作方式和落地验证，不写成接口手册。
- 修改公开能力、产品术语或核心流程时，同步更新 README、中英文 `docs/`、官网和文档站。
- 官网只描述已经实现或明确标注验证阶段的能力，示例数据必须清楚标识。

## 测试

提交前运行完整门禁：

```bash
./scripts/verify.sh
```

门禁包括：

- .NET 构建与单元、集成测试；
- Platform Web 构建、测试、Lint 和生产依赖审计；
- 官网与文档站静态构建、链接测试、Lint 和生产依赖审计；
- 架构依赖、Shell 语法、Compose 配置和差异格式检查。

新增行为至少包含正常路径、拒绝路径和权限边界测试。修复缺陷时先增加能够复现问题的测试。

## Pull Request

1. Fork 仓库并从最新 `main` 创建功能分支。
2. 保持变更范围单一，不混入无关格式化或重构。
3. 更新实现、测试和相关中英文文档。
4. 执行 `./scripts/verify.sh`。
5. 提交 Pull Request，并说明：
   - 问题与目标；
   - 对工艺研发流程的影响；
   - 公共契约或数据模型变化；
   - 安全与权限影响；
   - 验证结果；
   - 部署或配置要求。

普通缺陷和功能建议使用 [GitHub Issues](https://github.com/liuweichaox/Ingot/issues)。安全问题不得创建公开 Issue，请遵循[安全策略](SECURITY.md)。
